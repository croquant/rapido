{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // .tool_response.filePath // empty' | { read -r f; [[ \"$f\" == *.py ]] || exit 0; cd \"$CLAUDE_PROJECT_DIR\" && uv run ruff check --fix \"$f\"; uv run ruff format \"$f\"; uv run pyright \"$f\"; } 2>&1 || true"
          }
        ]
      }
    ]
  },
  "effortLevel": "xhigh"
}
